info:
  name: get_interests
  type: http
  seq: 3

http:
  method: GET
  url: http://localhost:8080/api/v1/users/me/interests
  headers:
    - name: Authorization
      value: Bearer {{customer_token}}
      enabled: true
  auth: none

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
