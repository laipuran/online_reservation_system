info:
  name: register
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:8080/api/v1/auth/register
  body:
    type: json
    data: |-
      {
        "email": "zhangsan@example.com",
        "password": "Abcd1234",
        "name": "张三",
        "role": "customer"
      }
  auth: none

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
