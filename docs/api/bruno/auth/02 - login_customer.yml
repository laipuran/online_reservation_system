info:
  name: login_customer
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:8080/api/v1/auth/login
  body:
    type: json
    data: |-
      {
        "email": "bruno_customer@example.com",
        "password": "123@abc123"
      }
  auth: none

runtime:
  scripts:
    - type: after-response
      code: |-
        const body = res.getBody();
        const token = body && body.data && body.data.access_token;
        if (token) {
          bru.setEnvVar("customer_token", token, { persist: true });
          bru.setVar("customer_token", token);
        }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
