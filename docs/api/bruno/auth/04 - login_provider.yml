info:
  name: login_provider
  type: http
  seq: 4

http:
  method: POST
  url: http://localhost:8080/api/v1/auth/login
  body:
    type: json
    data: |-
      {
        "email": "bruno_provider@example.com",
        "password": "123@abc123"
      }
  auth: none

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
