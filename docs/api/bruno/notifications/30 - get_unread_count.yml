info:
  name: get_unread_count
  type: http
  seq: 30

http:
  method: GET
  url: http://localhost:8080/api/v1/notifications/unread-count
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
