info:
  name: get_provider
  type: http
  seq: 14

http:
  method: GET
  url: http://localhost:8080/api/v1/providers/1
  auth: none

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
