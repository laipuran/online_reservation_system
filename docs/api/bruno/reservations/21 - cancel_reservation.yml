info:
  name: cancel_reservation
  type: http
  seq: 21

http:
  method: PUT
  url: http://localhost:8080/api/v1/reservations/1/cancel
  headers:
    - name: Authorization
      value: Bearer {{customer_token}}
      enabled: true
  auth: none

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
