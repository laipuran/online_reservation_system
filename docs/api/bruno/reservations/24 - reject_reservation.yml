info:
  name: reject_reservation
  type: http
  seq: 24

http:
  method: PUT
  url: http://localhost:8080/api/v1/provider/reservations/1/reject
  headers:
    - name: Authorization
      value: Bearer {{provider_token}}
      enabled: true
  auth: none

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
