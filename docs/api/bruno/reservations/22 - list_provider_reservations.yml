info:
  name: list_provider_reservations
  type: http
  seq: 22

http:
  method: GET
  url: "http://localhost:8080/api/v1/provider/reservations?status=pending"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
