info:
  name: get_reservation
  type: http
  seq: 20

http:
  method: GET
  url: http://localhost:8080/api/v1/reservations/{{reservation_id}}
  headers:
    - name: Authorization
      value: Bearer {{customer_token}}

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
