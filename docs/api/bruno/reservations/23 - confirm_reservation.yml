info:
  name: confirm_reservation
  type: http
  seq: 23

http:
  method: PUT
  url: http://localhost:8080/api/v1/provider/reservations/1/confirm
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
