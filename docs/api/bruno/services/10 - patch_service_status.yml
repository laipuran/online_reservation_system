info:
  name: patch_service_status
  type: http
  seq: 10

http:
  method: PATCH
  url: http://localhost:8080/api/v1/services/1/status
  body:
    type: json
    data: |-
      {
        "status": "inactive"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
